# Base URL of the Flask backend API.
# Copy this file to .env.local and adjust if the API runs elsewhere.
# Unset → defaults to http://localhost:8000 (see components/youtube-converter.tsx).
NEXT_PUBLIC_API_URL=http://localhost:8000
